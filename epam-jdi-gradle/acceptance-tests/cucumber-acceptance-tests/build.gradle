plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':tests-common')
}

test {
    // enable TestNG support (default is JUnit)
    useTestNG()

}
